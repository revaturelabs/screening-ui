import { Category } from './Category';
import { SkillType } from './SkillType';

export class Weight {
    weightId: number;
    weightValue: number;
    skillType: SkillType;
    category: Category;
}
