import { ToolbarFilterPipe } from './toolbar-filter.pipe';

describe('ToolbarFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ToolbarFilterPipe();
    expect(pipe).toBeTruthy();
  });
});