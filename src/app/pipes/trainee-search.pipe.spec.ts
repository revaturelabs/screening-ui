import { TraineeSearch } from './trainee-search.pipe';

describe('TraineeSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TraineeSearch();
    expect(pipe).toBeTruthy();
  });
});
