import { BucketFilterPipe } from './skillType-buckets.filter';

describe('BucketFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BucketFilterPipe();
    expect(pipe).toBeTruthy();
  });
});